# Generated by roxygen2: do not edit by hand

# S4 Class exports
exportClass(LocomotionData)

# S4 Generic and Method exports
exportMethods(
    initialize,
    show,
    summary,
    plot
)

# Define and export S4 generics
export(
    getCycles,
    getMeanPatterns,
    getStdPatterns,
    validateCycles,
    findOutlierCycles,
    getSummaryStatistics,
    calculateROM,
    getValidationReport,
    getSubjects,
    getTasks,
    getFeatures,
    plotPhasePatterns,
    plotTaskComparison,
    plotTimeSeries,
    getPhaseCorrelations,
    mergeWithTaskData,
    filterSubjects,
    filterTasks,
    clearCache,
    getMultiSubjectStatistics,
    getMultiTaskStatistics,
    getGroupMeanPatterns
)

# Export methods for the generics
exportMethods(
    getCycles,
    getMeanPatterns,
    getStdPatterns,
    validateCycles,
    findOutlierCycles,
    getSummaryStatistics,
    calculateROM,
    getValidationReport,
    getSubjects,
    getTasks,
    getFeatures,
    plotPhasePatterns,
    plotTaskComparison,
    plotTimeSeries,
    getPhaseCorrelations,
    mergeWithTaskData,
    filterSubjects,
    filterTasks,
    clearCache,
    getMultiSubjectStatistics,
    getMultiTaskStatistics,
    getGroupMeanPatterns
)

# Function exports
export(
    loadLocomotionData,
    efficientReshape3D,
    getFeatureConstants,
    getKinematicFeatureMap,
    getKineticFeatureMap,
    getVelocityFeatureMap,
    suggestStandardName,
    isStandardCompliant,
    loadParquetData,
    validateParquetStructure,
    detectDataFormat,
    convertTimeToPhase,
    getMemoryUsage,
    deg2rad,
    rad2deg,
    calculatePhase,
    interpolateToPhase,
    detectGaitEvents,
    createFeatureSummary,
    validateDataDimensions,
    formatFeatureName,
    run_all_examples,
    # Mixed-effects modeling functions
    prepare_mixed_effects_data,
    fit_hierarchical_model,
    fit_gait_analysis_model,
    fit_intervention_model,
    fit_group_comparison_model,
    compare_models,
    likelihood_ratio_test,
    recommend_random_effects,
    test_random_effects_structures,
    run_model_diagnostics,
    check_model_assumptions,
    calculate_effect_sizes,
    # Bayesian analysis functions
    bayes_ttest_biomech,
    bayes_anova_biomech,
    bayes_correlation_biomech,
    compare_bayes_models,
    interpret_bayes_factor,
    bayes_model_averaging,
    bayes_robustness_check,
    # Machine learning functions
    prepare_ml_data,
    biomech_pca,
    biomech_tsne,
    biomech_umap,
    biomech_kmeans,
    biomech_hierarchical,
    biomech_svm,
    biomech_random_forest,
    # ML workflow functions
    biomech_cross_validation,
    biomech_hyperparameter_tuning,
    biomech_feature_selection,
    biomech_model_comparison,
    biomech_ml_pipeline
)

# Import dependencies
import(methods)
importFrom(arrow, read_parquet)
importFrom(data.table, data.table, setDT, setkey)
importFrom(ggplot2, ggplot, aes, geom_line, geom_ribbon, facet_wrap, theme_minimal, labs, scale_color_manual, guide_legend)
importFrom(dplyr, filter, select, group_by, summarise, mutate, arrange, n)
importFrom(tibble, tibble, as_tibble)
importFrom(readr, read_csv)
importFrom(stats, sd, median, quantile, cor, na.omit, prcomp, dist, kmeans, aov, glm, t.test)
importFrom(utils, head, tail)
importFrom(magrittr, "%>%")
importFrom(lme4, lmer, fixef, ranef, VarCorr, ngrps)
importFrom(BayesFactor, ttestBF, anovaBF, correlationBF, extractBF, posterior)
importFrom(randomForest, randomForest)
importFrom(e1071, svm, tune)
importFrom(cluster, silhouette)